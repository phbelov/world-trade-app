{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @world-trade/web build && pnpm exec esbuild apps/api/src/serverless.ts --bundle --format=esm --platform=node --target=node22 --outfile=api-dist/server.mjs --external:@duckdb/node-api",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "regions": ["iad1"],
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/((?!api|assets|geo).*)", "destination": "/index.html" }
  ]
}
